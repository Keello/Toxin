mixin menu(values)
  nav.menu
    ul
      each val,key in values
        if(val=="")
          li.menu__item
            a= key
        else
          li.menu__item= key
            i.icon-expand_more
